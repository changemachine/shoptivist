<nav class="nav">
  <div class="container">
    <div class="logo">
      <h1>{{#link-to 'index'}}Shoptivist{{/link-to}}</h1>
    </div>
    <ul class="nav-main">
      {{#each categories as |category|}}
        <li>{{category.name}}
          <div class="drop-down-menu">
            <ul>
              {{fe-subcategory category=category}}
            </ul>
          </div>
        </li>
      {{/each}}
    </ul>
    <div class="search-prim">
      <input type="text" name="name" value="">
      <button class="button"><i class="fa fa-search"></i></button>
    </div>
  </div>
</nav>
