{{#if addNewSubCategory}}
<h1>Add Sub Category</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="name">name</label>
        {{input value=name id="name"}}
      </div>
      <button {{action 'saveSubCategory' category}}>Save</button>
    </form>
  </div>
 {{else}}
    <button class="btn btn-success"{{action 'subCategoryFormShow'}}>New SubCategory</button>
{{/if}}
